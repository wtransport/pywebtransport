[build-system]
requires = ["maturin>=1.14.1,<2.0"]
build-backend = "maturin"

[project]
name = "pywebtransport"
dynamic = ["version"]
description = "An async-native WebTransport stack for Python."
readme = "README.md"
license = "Apache-2.0"
license-files = ["LICENSE", "NOTICE"]
requires-python = ">=3.12"
authors = [{ name = "The WTransport Authors", email = "admin@wtransport.org" }]
keywords = [
    "async",
    "http3",
    "networking",
    "protocol",
    "quic",
    "rust",
    "webtransport",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Framework :: AsyncIO",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Programming Language :: Python :: Implementation :: CPython",
    "Programming Language :: Rust",
    "Topic :: Communications",
    "Topic :: Internet",
    "Topic :: Internet :: WWW/HTTP",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: System :: Networking",
    "Typing :: Typed",
]

[project.optional-dependencies]
dev = [
    "black>=26.5.1,<27.0.0",
    "flake8>=7.3.0,<8.0.0",
    "isort>=8.0.1,<9.0.0",
    "maturin[patchelf]>=1.14.1,<2.0",
    "mypy>=2.1.0,<3.0.0",
    "pywebtransport[test]",
]
docs = [
    "mkdocs>=1.6.1,<2.0.0",
    "mkdocs-material>=9.7.6,<10.0.0",
    "mkdocstrings[python]>=1.0.4,<2.0.0",
]
test = [
    "psutil>=7.2.2,<8.0.0",
    "pytest>=9.1.1,<10.0.0",
    "pytest-asyncio>=1.4.0,<2.0.0",
    "pytest-benchmark>=5.2.3,<6.0.0",
    "pytest-cov>=7.1.0,<8.0.0",
    "pytest-mock>=3.15.1,<4.0.0",
    "types-psutil>=7.2.2.20260518,<8.0.0",
    "uvloop>=0.22.1,<1.0.0",
]

[project.urls]
Changelog = "https://github.com/wtransport/pywebtransport/blob/main/CHANGELOG.md"
Documentation = "https://python.wtransport.org"
Funding = "https://opencollective.com/wtransport"
Homepage = "https://github.com/wtransport/pywebtransport"
Issues = "https://github.com/wtransport/pywebtransport/issues"
Repository = "https://github.com/wtransport/pywebtransport.git"

[tool.black]
line-length = 120
target-version = ["py312"]

[tool.coverage.report]
exclude_lines = [
    "@(abc\\.)?abstractmethod",
    "^\\s*\\.\\.\\.$",
    "class .*\\bProtocol\\):",
    "def __repr__",
    "if 0:",
    "if TYPE_CHECKING:",
    "if __name__ == .__main__.:",
    "if self.debug:",
    "if settings.DEBUG",
    "pragma: no cover",
    "raise AssertionError",
    "raise NotImplementedError",
]
show_missing = true
precision = 2

[tool.coverage.run]
source = ["src"]
branch = true
parallel = true
data_file = ".cache/coverage/.coverage"
omit = ["tests/*"]

[tool.isort]
profile = "black"
line_length = 120
known_first_party = ["pywebtransport"]

[tool.maturin]
module-name = "pywebtransport._pywebtransport"
features = ["pyo3/extension-module"]
manifest-path = "crates/Cargo.toml"
python-source = "src"
include = ["AUTHORS", "CHANGELOG.md"]

[tool.mypy]
mypy_path = "src"
python_version = "3.12"
show_error_codes = true
strict = true
warn_no_return = true
warn_unreachable = true

[[tool.mypy.overrides]]
module = ["pytest_benchmark.*"]
ignore_missing_imports = true

[tool.pytest.ini_options]
pythonpath = ["src"]
minversion = "9.0"
addopts = "-ra -q --strict-markers --strict-config"
testpaths = ["tests"]
norecursedirs = [
    "examples",
    "tests/benchmark",
    "tests/e2e",
    "tests/integration",
]
asyncio_mode = "strict"
markers = [
    "benchmark: Benchmark tests",
    "e2e: End-to-End tests",
    "integration: Integration tests",
    "unit: Unit tests",
]

[tool.tox]
requires = ["tox>=4"]
env_list = ["lint", "py312", "py313", "py314"]

[tool.tox.env_run_base]
description = "Test python ({env_name})"
package = "editable"
deps = "-r dev-requirements.txt"
commands = [
    [
        "pytest",
        "--cov=pywebtransport",
        "--cov-report=term",
        "{posargs}",
    ],
]

[tool.tox.env.lint]
description = "Run linters"
base_python = ["python3.14"]
package = "skip"
deps = "-r dev-requirements.txt"
commands = [
    [
        "isort",
        "--check-only",
        "src",
        "tests",
    ],
    [
        "black",
        "--check",
        "src",
        "tests",
    ],
    [
        "flake8",
        "src",
        "tests",
    ],
    [
        "mypy",
        "src",
        "tests",
    ],
]
