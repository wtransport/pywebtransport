# PyWebTransport Codecov Configuration

comment: false

coverage:
  round: down
  status:
    project:
      default:
        target: 100%
        threshold: 0%
    patch:
      default:
        target: 100%

ignore:
  - "crates/**/*"
  - "**/*.pyi"
