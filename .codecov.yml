# PyWebTransport Codecov Configuration

ignore:
  - "crates/**/*"
  - "**/*.pyi"

coverage:
  status:
    project:
      default:
        target: auto
        threshold: 5%
    patch:
      default:
        target: 90%
